# Scene: scene-04 — I2C

**Durum:** Planlanıyor (2026-07-16) — görsel HENÜZ ÜRETİLMEDİ, sadece prompt yazıldı.

**Gerçek görsel analizi (madde 4c):** Pin atamaları (PB6/PB7 I2C1, PB10/PB11 I2C2) datasheet
Table 5 ile doğrulandı (curriculum-qa, 2026-07-16). Hız iddiası QA'de düzeltildi: 100 kHz
Standard mode / 400 kHz Fast mode (önceki taslaktaki "1 MHz fast mode" STM32F103'ün I2C
biriminde desteklenmiyor). Aynı U2 şema sembolü referans olarak verilir:
`visual-system/references/real-u2-schematic-crop.png`.

**Kasıtlı görsel farklılaştırma:** scene-02 (UART, nokta-nokta) ve scene-03 (SPI, master/slave
çoklu-hat) ile aynı turuncu rengi paylaşır ama DİYAGRAM METAFORU farklı: I2C = ORTAK VERİYOLU
(shared bus) — tek bir yatay hat üzerinde birden fazla cihazın dallanarak bağlandığı bir
diyagram.

Üretim (henüz ÇALIŞTIRILMADI): `tools/generate-slide.py 10-iletisim-protokolleri/prompts/04-i2c.md 10-iletisim-protokolleri/slides/04-i2c.png --ref visual-system/references/real-u2-schematic-crop.png`

```text
SCENE:
Day label: "GÜN 10" — render with correct Turkish characters.
Main title: "I2C"
Subtitle: "2 kablo, adresli"

Central graphic: a SHARED BUS diagram (NOT a schematic symbol, NOT a point-to-point or
master/slave-box diagram) — one box labeled "Master" on the left, then TWO horizontal lines
("SDA" and "SCL") running rightward, with THREE small boxes branching off the same two lines
at different points along their length, labeled "Slave 1", "Slave 2", "Slave 3" — visually
showing all three slaves sharing the SAME two wires (not separate wires per slave, unlike
SPI's diagram). No colored border/frame around this central content.

Four NUMBERED panels (large colored number badge 1,2,3,4 top-left of each panel), each
connected with a thin matching-color line to the diagram, each panel exactly one cohesive icon
plus a caption of AT LEAST TWO concrete information lines. Use ONLY the exact values given
here, do not invent any additional pin or spec:

IMPORTANT — badge color: all four number badges (1,2,3,4) and all four connector lines and all
four panel borders MUST be the exact same solid orange color (the "belge/şema kaynağı"
function color, matching scene-02/03). Do NOT vary the badge color per panel.

IMPORTANT — each panel MUST use a clearly DIFFERENT icon matching its own content:
1 (orange, icon: two shared lines with multiple branch dots): "Nasıl Çalışır?" — "2 kablo:
SDA/SCL, senkron" / "Her slave'in 7-bit benzersiz adresi var"
2 (orange, icon: a small schematic pin-pair icon): "Şemada Nerede?" — "PB6/PB7 (I2C1)" /
"PB10/PB11 (I2C2 — USART3 ile paylaşımlı, biri seçilir)"
3 (orange, icon: a small EEPROM/sensor-chip icon): "Ne Zaman Kullanılır?" — "EEPROM
(AT24C256), sıcaklık sensörü (LM75)" / "IMU (MPU6050), barometrik sensör (BMP280)"
4 (orange, icon: a simple scale/balance icon): "Avantaj / Kısıt" — "Sadece 2 kablo, çok sayıda
slave" / "Yavaş (100 kHz / 400 kHz), pull-up dirençleri gerekli"

Do not render any pin number beyond PB6/PB7/PB10/PB11, and do not invent additional module
examples or a different I2C speed than the ones listed above.

Bottom section: use the mandatory bottom layout — 4 cards mirroring the panels: "Nasıl
Çalışır?", "Şemada Nerede?", "Ne Zaman Kullanılır?", "Avantaj/Kısıt", each with a simple icon
and short Turkish title. Then the same single mandatory footer row: bottom-left "Akademi
Usta", bottom-center "Usta tahmin etmez, ölçer.", bottom-right "akademiusta.com" — same bold
navy font, same baseline, text only, no logo/emblem.
```
